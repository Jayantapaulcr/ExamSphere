# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Increase JVM heap size for faster builds
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# Enable parallel project execution
org.gradle.parallel=true

# Enable build caching for faster incremental builds
org.gradle.caching=true

# Enable configuration caching for even faster subsequent builds
org.gradle.configuration-cache=true

# AndroidX package structure
android.useAndroidX=true

# Kotlin code style
kotlin.code.style=official

# Optimize R class generation
android.nonTransitiveRClass=true

# Experimental: Enable K2 compiler if not already enabled (Kotlin 2.0+ uses it by default, but this ensures it)
kotlin.incremental=true
